# AGENTS.md

本文件给进入本仓库工作的自动化代理使用。目标是减少误操作，并让改动方式与仓库当前习惯保持一致。

## 1. 仓库定位

- 这是一个 `nix-config` 仓库，同时覆盖 `NixOS`、`nix-darwin`、`home-manager`。
- 重点目录：
  - `hosts/`：主机入口
  - `modules/`：系统级模块
  - `home/`：用户级配置
  - `home/darwin/aerospace/`：macOS AeroSpace 配置
  - `documents/`：中文说明文档
  - `Justfile`：常用操作入口

## 2. 工作原则

- **必须使用中文回复**，除非用户明确要求其他语言。这是强制要求，不得违反。
- 处理问题时，优先参考网络文档、官方资料，以及优秀仓库中的相似设计；再结合本仓库内现有文档、`README.md`、`documents/`
  与实际代码结构落地修改。
- 所有配置变更都要先确认相关组件、发行版、模块或应用的版本号，并根据这些版本号在网上查找匹配的官方文档；排查问题时先查官方文档，再参考合适的教程、优秀仓库或社区方案。
- 根据文档和教程整理出合适方案后，应先与用户确认，再继续向下推导和落地修改。
- 先读上下文，再改文件；不要凭猜测改 Nix 结构。
- 优先做最小改动，避免无关重构。
- 改配置时，文档如果已经描述了该行为，应同步更新。
- 不要覆盖用户已有的未提交改动，除非用户明确要求。
- 不要运行破坏性 git 命令，如 `git reset --hard`、`git checkout --`。

## 3. 编辑约定

- 搜索优先使用 `rg`。
- 手工改文件时保持现有风格，不随意改排版或命名体系。
- 新增注释要短，只解释不明显的约束或原因。
- 除非用户要求，不要顺手重命名大段结构、移动目录或做“顺便清理”。

## 4. 验证原则

- 优先使用低风险验证：
  - `just test`
  - `nix eval .#evalTests --show-trace --print-build-logs --verbose`
- 涉及单个文件或文档的小改动，可只做静态检查并说明未跑完整验证。
- `just local`、`just local-host ...`
  会构建并切换当前系统，属于高影响操作；没有用户明确要求时不要执行。

## 5. Darwin / AeroSpace 相关

- AeroSpace 实际配置文件在 `home/darwin/aerospace/aerospace.toml`。
- 对应说明文档在 `documents/aerospace-usage.md`。
- 改快捷键、工作区、窗口规则时，优先同时更新这两处。
- 如果要验证 AeroSpace 行为，先确认 `AeroSpace.app` 是否真的可通信，不要只根据进程是否存在下结论。

## 6. 文档同步与变更记录

- 影响用户操作方式的改动，应同步更新 `README.md` 或 `documents/` 下对应说明。
- 所有新增、修改或移除配置的变更，都要留下完整文档记录；已有专题文档时同步更新，没有时在
  `documents/` 下新增对应文档。
- 必须维护 `documents/changelog.md`
  作为主线变更记录。每次配置变更都要按时间倒序追加记录，并引用对应的具体配置文档。
- `documents/changelog.md`
  只作为变更索引，记录变更主题、影响范围、配置入口、验证方式和关联文档；具体背景、当前行为、使用方式和回滚方式写入被引用的专题文档。
- 文档应直接写“当前配置如何工作”，不要保留已经失效的旧描述。

## 7. 提交前自检

- 改动是否只覆盖用户请求范围。
- 配置和文档是否一致。
- `documents/changelog.md` 是否已追加主线记录，并引用对应配置文档。
- 是否引入了需要用户手动执行的后续步骤；如果有，要明确写出。
- 是否运行了合适级别的验证；如果没有，要明确说明原因。
